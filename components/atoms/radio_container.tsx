import styled from "styled-components";

const RadioContainer = styled.div`
  margin: 24px 0;
  & p {
    font-weight: 600;
    margin-bottom: 3px;
  }
  & label {
    margin: 4px 0;
  }
`;

export default RadioContainer;
